# ProjectSettings

首次用 Unity 打开项目后，请在主机上确认：

- Version Control Mode: Visible Meta Files
- Asset Serialization Mode: Force Text
- Line Endings For New Scripts: OS Native 或 Unix

确认后提交 Unity 自动生成或更新的 `ProjectSettings/*.asset` 文件。
